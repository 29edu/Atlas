# Health Monitor

    It check all workers heartbeat
    Find tasks  stuck in 'processing' state
    Check if the worker processing the task is dead
    pushes orphaned tasks back to the queue.

# Understand the difference between forEach and for loop as i was using forEach loop in the code but it was wrong because it check all at onc